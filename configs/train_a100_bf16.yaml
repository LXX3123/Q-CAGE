_base_: qcage_base.yaml

training:
  output_dir: checkpoints/qcage_a100_bf16
  epochs: 3
  max_steps: null
  batch_size_per_gpu: 1
  gradient_accumulation_steps: 8
  learning_rate: 1.0e-4
  weight_decay: 0.01
  precision: bf16
  log_every: 10
  save_every: 1000

data:
  use_cached_features: true
  num_workers: 8

